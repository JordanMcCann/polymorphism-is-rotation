# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
*.egg
.eggs/

# uv / venv
.venv/
venv/
.python-version

# build artifacts
build/
dist/
*.whl

# GitHub Release attachment, regenerated by scripts/build_github_release.py
polymorphism-replication-lite.tar.gz

# pytest / coverage
.pytest_cache/
.coverage
htmlcov/
.tox/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# the heavy artifacts live here; they are downloaded by `replicate fetch-artifacts`
# (leading slash = repo root only, so it never matches a source dir of the same name)
/artifacts/

# the legacy `experiments/` path is a symlink to `artifacts/`; skip it.
# Leading slash anchors to repo root so this does NOT match the
# src/polymorphism/experiments/ source subpackage.
/experiments

# upload log (contains nothing sensitive but is ephemeral)
hf_upload.log
hf_upload.err
*.log

# LaTeX build artifacts in paper/
paper/main.aux
paper/main.log
paper/main.out
paper/main.blg
paper/*.synctex.gz
